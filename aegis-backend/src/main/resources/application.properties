spring.application.name=aegis-backend
spring.datasource.url=jdbc:postgresql://localhost:5432/aegis_db
spring.datasource.username=aegis_user
spring.datasource.password=local_password_123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA (Validate = não mexe na estrutura, deixa pro Flyway)
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Flyway
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration

app.service-token=dev-token-123
